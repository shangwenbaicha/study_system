# 📚 学习系统 — 开发文档

> 三大核心模块：课程表 · 学习计划 · 日程安排  
> 全局公共能力：日历总览 · 数据分析 · 备份管理 · 主题设置

---

## 一、项目概览

一个面向学生的全功能学习管理系统，采用**前后端分离架构**，三个核心模块**完全隔离、无功能重叠**，通过全局公共模块提供统一能力。

### 架构图

```
┌─────────────────────────────────────────────────────┐
│                    前端 (React + Vite)                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 课程表    │  │ 学习计划  │  │ 日程安排  │          │
│  │ 页面      │  │ 页面      │  │ 页面      │          │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘          │
│       └──────────┬──┴─────────────┘                 │
│                  ▼                                   │
│  ┌─────────────────────────────────────────────┐    │
│  │ 全局页面：日历总览 / 数据分析 / 备份 / 设置  │    │
│  └─────────────────────────────────────────────┘    │
└──────────────────────┬──────────────────────────────┘
                       │ RESTful API
┌──────────────────────▼──────────────────────────────┐
│              后端 (Express + Prisma)                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 课程 CRUD │  │ 学习 CRUD │  │ 日程 CRUD │          │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘          │
│       └──────────┬──┴─────────────┘                 │
│                  ▼                                   │
│  ┌─────────────────────────────────────────────┐    │
│  │ 日历引擎 / 分析引擎 / 备份引擎 / PDF 引擎    │    │
│  └─────────────────────────────────────────────┘    │
└──────────────────────┬──────────────────────────────┘
                       ▼
              ┌────────────────┐
              │   SQLite 数据库  │
              └────────────────┘
```

---

## 二、模块严格边界

### 课程表（Schedule Course）
| 项目 | 内容 |
|------|------|
| **职责** | 学期固定上课安排 |
| **不含** | 自主学习规划、日常琐事日程、任务打卡 |
| **核心字段** | 课程名称、授课老师、教室、星期、节次、上课周数(1-20)、单双周、课程颜色、备注、作业截止、考试时间 |
| **专属功能** | 课时统计、空课自动识别、单双周排课、课前提醒、整周复制、学期重置 |

### 学习计划表（Study Plan）
| 项目 | 内容 |
|------|------|
| **职责** | 自主学习目标、每日学习任务、备考规划、打卡复盘 |
| **不含** | 学校固定课程、生活约会/办事日程 |
| **核心字段** | 学习科目、目标周期、每日计划时长、任务明细、完成状态、开始/截止日期、优先级、关联笔记、复习周期 |
| **专属功能** | 阶段目标设定、艾宾浩斯复习、番茄钟、任务顺延、学习模板、科目打卡、薄弱科目分析 |

### 日程安排表（Daily Agenda）
| 项目 | 内容 |
|------|------|
| **职责** | 生活、工作、约会、截止日期、临时事务 |
| **不含** | 排课、系统性学习规划 |
| **核心字段** | 日程标题、开始/结束时间、地点、标签分类、重复规则、提醒时间、优先级、备注、完成状态 |
| **专属功能** | 每日待办+日程合并、循环日程、多重提醒、过期自动归档、节假日标注、忙碌空闲分析 |

---

## 三、技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | React | ^19.2.0 |
| 路由 | React Router DOM | ^7.6.0 |
| 构建工具 | Vite | ^7.1.10 |
| 图表 | Recharts | ^2.15.3 |
| 图标 | Lucide React | ^0.510.0 |
| 日期处理 | Day.js | ^1.11.13 |
| 后端框架 | Express | ^4.21.0 |
| ORM | Prisma | ^6.6.0 |
| 数据库 | SQLite | |
| 语言 | TypeScript | ^5.8.0 |
| 包管理 | npm workspaces (Monorepo) | |

---

## 四、目录结构

```
study_system/
├── 开发.md                          # 本开发文档
├── package.json                     # Monorepo 根配置
├── tsconfig.json                    # 全局 TypeScript 配置
├── tsconfig.api.json                # API 专用 TS 配置
├── start.bat                        # 一键启动脚本
├── .env                             # 环境变量
├── .env.example                     # 环境变量模板
├── .gitignore
│
├── prisma/
│   └── schema.prisma                # 数据库模型定义（5 个核心模型）
│
├── packages/
│   ├── domain/                      # 领域层
│   │   └── src/
│   │       ├── types.ts             # 核心类型定义
│   │       └── contracts.ts         # 接口契约
│   │
│   └── common-engines/              # 公共引擎
│       └── src/
│           └── interfaces.ts        # 日历/PDF/图表/通知引擎接口
│
├── services/
│   └── api/                         # 后端 API 服务
│       └── src/
│           ├── server.ts            # Express 入口
│           ├── app-service.ts       # 应用服务层
│           ├── db-service.ts        # 数据库 CRUD + 分析 + 备份
│           └── modules.ts           # API 路由模块
│
├── apps/
│   └── web/                         # 前端应用
│       ├── index.html
│       ├── vite.config.ts
│       ├── package.json
│       └── src/
│           ├── main.tsx             # 入口
│           ├── App.tsx              # 路由 + 布局 + 侧边栏
│           ├── index.css            # 全局样式（CSS 变量主题系统）
│           ├── vite-env.d.ts        # Vite 类型声明
│           ├── api/
│           │   └── client.ts        # API 客户端（7 个模块）
│           └── pages/
│               ├── course/
│               │   └── CoursePage.tsx    # 课程表页面
│               ├── study/
│               │   └── StudyPage.tsx     # 学习计划页面
│               ├── agenda/
│               │   └── AgendaPage.tsx    # 日程安排页面
│               └── global/
│                   ├── CalendarPage.tsx  # 日历总览
│                   ├── AnalyticsPage.tsx # 数据分析
│                   ├── BackupPage.tsx    # 备份管理
│                   └── SettingsPage.tsx  # 设置
│
└── tests/
    └── architecture.spec.ts         # 架构测试
```

---

## 五、数据库设计（Prisma Schema）

5 个核心模型 + 2 个基础模型：

### 基础模型
- **User** — 用户（id, name, email）
- **Workspace** — 工作区（id, name, userId）

### 核心模型
1. **CourseItem** — 课程条目
   - courseName, teacher, classroom, weekday(1-7), period, weeks(JSON), oddEvenRule, color, note, assignmentDueAt, examAt, remindBeforeMinutes, semesterId

2. **StudyTask** — 学习任务
   - subject, subjectId, targetCycle, plannedMinutes, taskDetail, status(todo/doing/done), startDate, dueDate, priority, relatedNote, reviewCycleDays, pomodoroCount, checkInDates(JSON)

3. **AgendaItem** — 日程条目
   - title, startAt, endAt, location, category, tags(JSON), recurrenceRule, reminderAt, priority, note, status(todo/done), archivedAt

4. **Subject** — 科目
   - name, color, isWeak

5. **Semester** — 学期
   - name, startDate, endDate, weekCount

---

## 六、API 接口

| 路由模块 | 端点 | 方法 | 说明 |
|---------|------|------|------|
| **课程** | `/api/courses` | GET/POST | 列表/创建 |
| | `/api/courses/:id` | GET/PUT/DELETE | 详情/更新/删除 |
| **学期** | `/api/semesters` | GET/POST | 列表/创建 |
| | `/api/semesters/:id` | PUT/DELETE | 更新/删除 |
| **科目** | `/api/subjects` | GET/POST | 列表/创建 |
| | `/api/subjects/:id` | PUT/DELETE | 更新/删除 |
| **学习任务** | `/api/study-tasks` | GET/POST | 列表/创建 |
| | `/api/study-tasks/:id` | GET/PUT/DELETE | 详情/更新/删除 |
| **日程** | `/api/agenda-items` | GET/POST | 列表/创建 |
| | `/api/agenda-items/:id` | GET/PUT/DELETE | 详情/更新/删除 |
| | `/api/agenda-items/:id/archive` | POST | 归档 |
| **日历** | `/api/calendar/events` | GET | 获取事件（支持模块筛选+时间范围） |
| **分析** | `/api/analytics/course` | GET | 课程分析 |
| | `/api/analytics/study` | GET | 学习分析 |
| | `/api/analytics/agenda` | GET | 日程分析 |
| **备份** | `/api/backup/export` | POST | 导出备份 |
| | `/api/backup/import` | POST | 导入备份 |
| | `/api/backup/archive` | POST | 创建归档 |

---

## 七、前端页面功能

| 页面 | 路由 | 功能 |
|------|------|------|
| **课程表** | `/course` | 周课表网格视图、课程列表、添加/编辑弹窗、周数选择、单双周、颜色选择、上下周切换 |
| **学习计划** | `/study` | 任务列表、状态切换(todo/doing/done)、统计卡片、筛选标签、番茄钟计数、复习周期 |
| **日程安排** | `/agenda` | 待办/已完成/已归档、分类标签、重复规则、提醒时间、优先级、地点 |
| **日历总览** | `/calendar` | 月视图、模块筛选(课程/学习/日程可单独或叠加)、今日事件列表 |
| **数据分析** | `/analytics` | 课程分析(课时占比饼图+柱状图)、学习分析(完成率+薄弱科目)、日程分析(分类占比) |
| **备份管理** | `/backup` | 导出(可选范围+加密)、导入、创建归档 |
| **设置** | `/settings` | 深色/浅色模式、8 种主题色切换 |

---

## 八、启动方式

### 一键启动
```bash
start.bat
```

### 手动启动

**1. 安装依赖**
```bash
npm install
```

**2. 初始化数据库**
```bash
npx prisma generate
npx prisma db push
```

**3. 启动后端（端口 3000）**
```bash
npx tsx services/api/src/server.ts
```

**4. 启动前端（端口 5173）**
```bash
cd apps/web
npx vite --host
```

**5. 访问**
- 前端：http://localhost:5173
- 后端 API：http://localhost:3000

---

## 九、全局公共能力

### PDF 导出引擎（接口已定义，待实现具体渲染）
- 可选版式：简约版、正式版、学生版
- 可导出：单页、整周、整月、整年、分析报告

### 图表生成（已实现 Recharts 集成）
- 饼图：分类占比
- 柱状图：时长/数量对比
- 折线图：每日趋势变化

### 日历引擎（已实现）
- 三个模块数据分别渲染到日历
- 可单独只看课程/只看学习/只看日程/全部叠加

### 数据分析引擎（已实现）
- 课程：总课时、空闲时段、各科占比
- 学习：日均时长、完成率、坚持天数、薄弱科目
- 日程：忙碌天数、事务分类占比、空闲时间统计

### 备份/还原/归档（已实现）
- 本地数据自动备份
- 手动导出备份文件（支持加密）
- 数据归档快照

### 主题设置（已实现）
- 深色/浅色模式
- 8 种自定义配色

---

## 十、后续迭代建议

### 短期优化（1-2 周）
1. **PDF 导出** — 实现 `PdfEngine` 的具体渲染（可使用 `jspdf` 或 `html2canvas`）
2. **全局消息提醒** — 实现 `NotificationEngine`，支持浏览器 Notification API
3. **番茄钟** — 前端实现番茄钟计时器组件
4. **艾宾浩斯复习** — 根据 `reviewCycleDays` 自动生成复习计划

### 中期功能（1-2 月）
5. **模板中心** — 预设课表模板、学习计划模板、日程模板
6. **数据导入** — 支持从 Excel/iCal 导入课程表
7. **移动端适配** — 完善响应式布局
8. **用户认证** — 登录/注册、多用户隔离

### 长期规划
9. **PWA 支持** — 离线可用、桌面图标
10. **云端同步** — 多设备数据同步
11. **AI 学习建议** — 基于数据分析推荐学习计划
12. **第三方日历同步** — Google Calendar / Outlook 双向同步

---

## 十一、开发规范

### 代码风格
- TypeScript 严格模式
- 使用 `const` / `let`，避免 `var`
- 异步操作使用 `async/await`
- 组件使用函数组件 + Hooks

### 命名规范
- 文件：PascalCase（组件）、camelCase（工具函数）
- 接口：`XxxDTO`（数据传输对象）
- 类型：`XxxType`、`XxxStatus`
- API 方法：`资源Api.操作`（如 `courseApi.list()`）

### 模块隔离原则
- 课程表代码不引用学习计划或日程的类型
- 学习计划代码不引用课程表或日程的类型
- 日程代码不引用课程表或学习计划的类型
- 全局模块可以引用所有类型，但只做展示不做修改

---

> 最后更新：2026-05-11  
> 技术栈：React 19 + TypeScript 5.8 + Prisma 6 + SQLite + Vite 7
